{% load static %}

<!-- 1. The <iframe> (and video player) will replace this <div> tag. -->
<div id="player_{{ video.id }}" class="yt-player aden-movie-fullscreen"
      width="100vw" height="100vh"
      data-videoId="{{ movie.youtube_api }}">
</div>
