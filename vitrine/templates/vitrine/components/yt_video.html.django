<!-- 1. The <iframe> (and video player) will replace this <div> tag. -->
<div id="player_{{ video.id }}" class="yt-player"
      width="100%" height="100%"
      data-videoId="{{ video.youtube_api }}">
</div>
